import 'package:flutter/cupertino.dart';

abstract class Borders {
  static const BorderRadius radius4All = BorderRadius.all(Radius.circular(4.0));
  static const BorderRadius radius8All = BorderRadius.all(Radius.circular(8.0));
  static const BorderRadius radius10All = BorderRadius.all(Radius.circular(10.0));
}
